# =============================================================================
# PayPing dashboard — environment variables
#
# Copy this file to `.env` and fill in real values:
#   cp .env.example .env
# =============================================================================

# Base URL of the PayPing API, including the /api prefix.
# In local dev the API listens on port 5000 by default.
VITE_API_URL=http://localhost:5000/api